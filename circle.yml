machine:
  services:
    - docker

dependencies:
  override:
    - docker version
    - docker info
    - docker build -t="blacklabelops/centos" .
    - docker images

test:
  override:
    - docker run -d --name="centosbase" blacklabelops/centos bash
    - sleep 10
    - docker stop centosbase
    - docker rm centosbase
    - docker rmi blacklabelops/centos
